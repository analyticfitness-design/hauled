# Módulo 3: Auth API — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans

**Goal:** Endpoints de registro, login, logout y /me funcionando con Sanctum. Verificados con curl.

**Architecture:** AuthController maneja los 4 endpoints. Sanctum genera tokens Bearer. Request classes validan los datos de entrada.

**Tech Stack:** Laravel Sanctum, FormRequest, Resource classes

---

### Task 1: Request classes de validación

**Files:**
- Create: `app/Http/Requests/Auth/RegisterRequest.php`
- Create: `app/Http/Requests/Auth/LoginRequest.php`

- [ ] **Step 1: Crear RegisterRequest**

```bash
cd C:/Users/GODSF/Herd/hauled-api
php artisan make:request Auth/RegisterRequest
```

Editar `app/Http/Requests/Auth/RegisterRequest.php`:
```php
<?php

namespace App\Http\Requests\Auth;

use Illuminate\Foundation\Http\FormRequest;

class RegisterRequest extends FormRequest
{
    public function authorize(): bool
    {
        return true;
    }

    public function rules(): array
    {
        return [
            'name' => ['required', 'string', 'max:255'],
            'email' => ['required', 'email', 'unique:users,email'],
            'password' => ['required', 'string', 'min:8', 'confirmed'],
            'phone' => ['nullable', 'string', 'max:20'],
        ];
    }
}
```

- [ ] **Step 2: Crear LoginRequest**

```bash
php artisan make:request Auth/LoginRequest
```

```php
<?php

namespace App\Http\Requests\Auth;

use Illuminate\Foundation\Http\FormRequest;

class LoginRequest extends FormRequest
{
    public function authorize(): bool
    {
        return true;
    }

    public function rules(): array
    {
        return [
            'email' => ['required', 'email'],
            'password' => ['required', 'string'],
        ];
    }
}
```

---

### Task 2: UserResource

**Files:**
- Create: `app/Http/Resources/UserResource.php`

- [ ] **Step 1: Crear UserResource**

```bash
php artisan make:resource UserResource
```

Editar `app/Http/Resources/UserResource.php`:
```php
<?php

namespace App\Http\Resources;

use Illuminate\Http\Request;
use Illuminate\Http\Resources\Json\JsonResource;

class UserResource extends JsonResource
{
    public function toArray(Request $request): array
    {
        return [
            'id' => $this->id,
            'name' => $this->name,
            'email' => $this->email,
            'phone' => $this->phone,
            'address' => $this->address,
            'role' => $this->role,
            'created_at' => $this->created_at->toDateString(),
        ];
    }
}
```

---

### Task 3: AuthController

**Files:**
- Create: `app/Http/Controllers/Api/AuthController.php`

- [ ] **Step 1: Crear AuthController**

```bash
php artisan make:controller Api/AuthController
```

Editar `app/Http/Controllers/Api/AuthController.php`:
```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Http\Requests\Auth\LoginRequest;
use App\Http\Requests\Auth\RegisterRequest;
use App\Http\Resources\UserResource;
use App\Models\User;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Hash;
use Illuminate\Validation\ValidationException;

class AuthController extends Controller
{
    public function register(RegisterRequest $request): JsonResponse
    {
        $user = User::create([
            'name' => $request->name,
            'email' => $request->email,
            'password' => Hash::make($request->password),
            'phone' => $request->phone,
            'role' => 'user',
        ]);

        $token = $user->createToken('hauled-app')->plainTextToken;

        return response()->json([
            'user' => new UserResource($user),
            'token' => $token,
        ], 201);
    }

    public function login(LoginRequest $request): JsonResponse
    {
        $user = User::where('email', $request->email)->first();

        if (! $user || ! Hash::check($request->password, $user->password)) {
            throw ValidationException::withMessages([
                'email' => ['Credenciales incorrectas.'],
            ]);
        }

        // Revocar tokens anteriores del dispositivo
        $user->tokens()->where('name', 'hauled-app')->delete();

        $token = $user->createToken('hauled-app')->plainTextToken;

        return response()->json([
            'user' => new UserResource($user),
            'token' => $token,
        ]);
    }

    public function logout(Request $request): JsonResponse
    {
        $request->user()->currentAccessToken()->delete();

        return response()->json(['message' => 'Sesión cerrada correctamente.']);
    }

    public function me(Request $request): JsonResponse
    {
        return response()->json([
            'user' => new UserResource($request->user()),
        ]);
    }
}
```

---

### Task 4: Rutas de Auth

**Files:**
- Modify: `routes/api.php`

- [ ] **Step 1: Agregar rutas de auth en routes/api.php**

```php
<?php

use App\Http\Controllers\Api\AuthController;
use Illuminate\Support\Facades\Route;

Route::prefix('v1')->group(function () {

    // Health check
    Route::get('/health', function () {
        return response()->json([
            'status' => 'ok',
            'app' => config('app.name'),
            'version' => '1.0.0',
        ]);
    });

    // Auth
    Route::prefix('auth')->group(function () {
        Route::post('/register', [AuthController::class, 'register']);
        Route::post('/login', [AuthController::class, 'login']);

        Route::middleware('auth:sanctum')->group(function () {
            Route::post('/logout', [AuthController::class, 'logout']);
            Route::get('/me', [AuthController::class, 'me']);
        });
    });

});
```

---

### Task 5: Verificar con curl

- [ ] **Step 1: Probar registro**

```bash
curl -X POST http://hauled-api.test/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{"name":"Juan Test","email":"juan@test.com","password":"password","password_confirmation":"password"}'
```

Esperado (201):
```json
{
  "user": {"id": 3, "name": "Juan Test", "email": "juan@test.com", "role": "user"},
  "token": "1|xxxxxxxxxxxxxxxx"
}
```

- [ ] **Step 2: Probar login**

```bash
curl -X POST http://hauled-api.test/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{"email":"admin@hauled.co","password":"password"}'
```

Esperado (200): user con `"role":"admin"` + token

- [ ] **Step 3: Guardar el token y probar /me**

Reemplazar TOKEN con el token recibido:
```bash
curl http://hauled-api.test/api/v1/auth/me \
  -H "Authorization: Bearer TOKEN" \
  -H "Accept: application/json"
```

Esperado: Datos del usuario logueado.

- [ ] **Step 4: Probar logout**

```bash
curl -X POST http://hauled-api.test/api/v1/auth/logout \
  -H "Authorization: Bearer TOKEN" \
  -H "Accept: application/json"
```

Esperado: `{"message":"Sesión cerrada correctamente."}`

- [ ] **Step 5: Verificar que token invalidado ya no funciona**

```bash
curl http://hauled-api.test/api/v1/auth/me \
  -H "Authorization: Bearer TOKEN" \
  -H "Accept: application/json"
```

Esperado (401): `{"message":"Unauthenticated."}`

- [ ] **Step 6: Probar login con credenciales incorrectas**

```bash
curl -X POST http://hauled-api.test/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{"email":"admin@hauled.co","password":"incorrecta"}'
```

Esperado (422): `{"message":"Credenciales incorrectas.",...}`

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: auth API completa (register, login, logout, me) con Sanctum"
```

---

### Verificación Final del Módulo 3

- [ ] POST `/api/v1/auth/register` → 201 con token
- [ ] POST `/api/v1/auth/login` → 200 con token
- [ ] GET `/api/v1/auth/me` con token → 200 con datos del usuario
- [ ] POST `/api/v1/auth/logout` con token → 200
- [ ] GET `/api/v1/auth/me` con token revocado → 401
- [ ] POST `/api/v1/auth/login` con clave incorrecta → 422

**Módulo 3 completado. Avanzar a Módulo 4.**
