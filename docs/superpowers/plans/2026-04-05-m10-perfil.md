# Módulo 10: Perfil de Usuario — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans

**Goal:** Página de perfil en Nuxt completamente funcional: datos personales editables, historial de pedidos con estados, historial de encargos con tracking, cambio de contraseña.

**Architecture:** ProfileController en Laravel maneja updates. Nuxt profile-area.vue usa tabs para seccionar: Info Personal, Mis Pedidos, Mis Encargos, Seguridad.

**Tech Stack:** Laravel, Nuxt, Pinia authStore

---

### Task 1: ProfileController en Laravel

**Files:**
- Create: `app/Http/Controllers/Api/ProfileController.php`
- Create: `app/Http/Requests/UpdateProfileRequest.php`
- Create: `app/Http/Requests/UpdatePasswordRequest.php`

- [ ] **Step 1: Crear UpdateProfileRequest**

```bash
cd C:/Users/GODSF/Herd/hauled-api
php artisan make:request UpdateProfileRequest
```

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Validation\Rule;

class UpdateProfileRequest extends FormRequest
{
    public function authorize(): bool { return true; }

    public function rules(): array
    {
        return [
            'name' => ['sometimes', 'string', 'max:255'],
            'phone' => ['sometimes', 'nullable', 'string', 'max:20'],
            'address' => ['sometimes', 'nullable', 'array'],
            'address.street' => ['sometimes', 'string'],
            'address.city' => ['sometimes', 'string'],
            'address.department' => ['sometimes', 'string'],
            'email' => [
                'sometimes',
                'email',
                Rule::unique('users')->ignore($this->user()->id),
            ],
        ];
    }
}
```

- [ ] **Step 2: Crear UpdatePasswordRequest**

```bash
php artisan make:request UpdatePasswordRequest
```

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class UpdatePasswordRequest extends FormRequest
{
    public function authorize(): bool { return true; }

    public function rules(): array
    {
        return [
            'current_password' => ['required', 'string'],
            'password' => ['required', 'string', 'min:8', 'confirmed'],
        ];
    }
}
```

- [ ] **Step 3: Crear ProfileController**

```bash
php artisan make:controller Api/ProfileController
```

```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Http\Requests\UpdatePasswordRequest;
use App\Http\Requests\UpdateProfileRequest;
use App\Http\Resources\EncargoResource;
use App\Http\Resources\OrderResource;
use App\Http\Resources\UserResource;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Hash;
use Illuminate\Validation\ValidationException;

class ProfileController extends Controller
{
    public function update(UpdateProfileRequest $request): JsonResponse
    {
        $user = $request->user();
        $user->update($request->validated());

        return response()->json([
            'user' => new UserResource($user),
            'message' => 'Perfil actualizado correctamente.',
        ]);
    }

    public function updatePassword(UpdatePasswordRequest $request): JsonResponse
    {
        $user = $request->user();

        if (! Hash::check($request->current_password, $user->password)) {
            throw ValidationException::withMessages([
                'current_password' => ['La contraseña actual no es correcta.'],
            ]);
        }

        $user->update(['password' => Hash::make($request->password)]);

        return response()->json(['message' => 'Contraseña actualizada correctamente.']);
    }

    public function orders(Request $request): JsonResponse
    {
        $orders = $request->user()
            ->orders()
            ->with('items')
            ->orderBy('created_at', 'desc')
            ->get();

        return response()->json(['data' => OrderResource::collection($orders)]);
    }

    public function encargos(Request $request): JsonResponse
    {
        $encargos = $request->user()
            ->encargos()
            ->orderBy('created_at', 'desc')
            ->get();

        return response()->json(['data' => EncargoResource::collection($encargos)]);
    }
}
```

- [ ] **Step 4: Agregar rutas de perfil en api.php**

Dentro del grupo `auth:sanctum`:
```php
use App\Http\Controllers\Api\ProfileController;

Route::put('/profile', [ProfileController::class, 'update']);
Route::put('/profile/password', [ProfileController::class, 'updatePassword']);
Route::get('/profile/orders', [ProfileController::class, 'orders']);
Route::get('/profile/encargos', [ProfileController::class, 'encargos']);
```

- [ ] **Step 5: Verificar con curl**

```bash
TOKEN="el-token-del-login"

# Ver pedidos del usuario
curl http://hauled-api.test/api/v1/profile/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "Accept: application/json"

# Actualizar perfil
curl -X PUT http://hauled-api.test/api/v1/profile \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{"name":"Cliente Actualizado","phone":"3100000000"}'
```

---

### Task 2: Filament UserResource

**Files:**
- Create: `app/Filament/Resources/UserResource.php`

- [ ] **Step 1: Generar UserResource**

```bash
php artisan make:filament-resource User --generate
```

Editar `app/Filament/Resources/UserResource.php`:
```php
<?php

namespace App\Filament\Resources;

use App\Filament\Resources\UserResource\Pages;
use App\Models\User;
use Filament\Forms\Form;
use Filament\Resources\Resource;
use Filament\Tables;
use Filament\Tables\Table;

class UserResource extends Resource
{
    protected static ?string $model = User::class;
    protected static ?string $navigationIcon = 'heroicon-o-users';
    protected static ?string $navigationLabel = 'Usuarios';
    protected static ?string $modelLabel = 'Usuario';
    protected static ?string $pluralModelLabel = 'Usuarios';
    protected static ?int $navigationSort = 6;

    public static function form(Form $form): Form
    {
        return $form->schema([]);
    }

    public static function table(Table $table): Table
    {
        return $table
            ->columns([
                Tables\Columns\TextColumn::make('name')->label('Nombre')->searchable(),
                Tables\Columns\TextColumn::make('email')->label('Email')->searchable(),
                Tables\Columns\TextColumn::make('phone')->label('Teléfono')->default('—'),
                Tables\Columns\BadgeColumn::make('role')
                    ->label('Rol')
                    ->colors(['warning' => 'admin', 'primary' => 'user']),
                Tables\Columns\TextColumn::make('orders_count')
                    ->label('Pedidos')
                    ->counts('orders'),
                Tables\Columns\TextColumn::make('encargos_count')
                    ->label('Encargos')
                    ->counts('encargos'),
                Tables\Columns\TextColumn::make('created_at')
                    ->label('Registro')
                    ->dateTime('d/m/Y'),
            ])
            ->filters([
                Tables\Filters\SelectFilter::make('role')
                    ->options(['admin' => 'Admin', 'user' => 'Usuario']),
            ])
            ->actions([
                Tables\Actions\ViewAction::make(),
            ])
            ->defaultSort('created_at', 'desc');
    }

    public static function getPages(): array
    {
        return [
            'index' => Pages\ListUsers::route('/'),
            'view' => Pages\ViewUser::route('/{record}'),
        ];
    }
}
```

---

### Task 3: Página de Perfil en Nuxt

**Files:**
- Modify: `C:/Users/GODSF/Herd/Hauled/app/components/profile/profile-area.vue`
- Modify: `C:/Users/GODSF/Herd/Hauled/app/components/profile/profile-info.vue`
- Modify: `C:/Users/GODSF/Herd/Hauled/app/components/profile/profile-orders.vue`
- Modify: `C:/Users/GODSF/Herd/Hauled/app/components/profile/profile-password.vue`

- [ ] **Step 1: Actualizar profile-info.vue con formulario funcional**

```vue
<script setup lang="ts">
import { toast } from 'vue3-toastify'

const authStore = useAuthStore()
const { apiFetch } = useApi()

const form = ref({
  name: authStore.user?.name ?? '',
  email: authStore.user?.email ?? '',
  phone: authStore.user?.phone ?? '',
  address: authStore.user?.address ?? { street: '', city: '', department: '' },
})

const loading = ref(false)

const handleUpdate = async () => {
  loading.value = true
  try {
    const res = await apiFetch<{ user: any; message: string }>('/profile', {
      method: 'PUT',
      body: form.value,
    })
    // Actualizar store con datos nuevos
    authStore.user = res.user
    authStore.saveToStorage()
    toast.success(res.message)
  } catch (err: any) {
    toast.error(err?.data?.message ?? 'Error al actualizar.')
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <form @submit.prevent="handleUpdate">
    <div class="row g-3">
      <div class="col-md-6">
        <label class="form-label">Nombre completo</label>
        <input v-model="form.name" type="text" class="form-control" required />
      </div>
      <div class="col-md-6">
        <label class="form-label">Email</label>
        <input v-model="form.email" type="email" class="form-control" required />
      </div>
      <div class="col-md-6">
        <label class="form-label">Teléfono</label>
        <input v-model="form.phone" type="tel" class="form-control" />
      </div>
      <div class="col-12">
        <label class="form-label">Dirección</label>
        <input v-model="form.address.street" type="text" placeholder="Calle / Carrera" class="form-control mb-2" />
        <div class="row g-2">
          <div class="col">
            <input v-model="form.address.city" type="text" placeholder="Ciudad" class="form-control" />
          </div>
          <div class="col">
            <input v-model="form.address.department" type="text" placeholder="Departamento" class="form-control" />
          </div>
        </div>
      </div>
      <div class="col-12">
        <button type="submit" :disabled="loading" class="tp-btn">
          {{ loading ? 'Guardando...' : 'Guardar cambios' }}
        </button>
      </div>
    </div>
  </form>
</template>
```

- [ ] **Step 2: Actualizar profile-orders.vue con datos reales**

```vue
<script setup lang="ts">
const { apiFetch } = useApi()

const { data } = await useAsyncData('profile-orders', () =>
  apiFetch<{ data: any[] }>('/profile/orders')
)

const orders = computed(() => data.value?.data ?? [])

const statusLabel = (status: string) => ({
  pending: '⏳ Pendiente',
  paid: '✅ Pagado',
  shipped: '🚚 Enviado',
  delivered: '📦 Entregado',
  cancelled: '❌ Cancelado',
}[status] ?? status)

const statusColor = (status: string) => ({
  pending: 'warning',
  paid: 'success',
  shipped: 'info',
  delivered: 'primary',
  cancelled: 'danger',
}[status] ?? 'secondary')
</script>

<template>
  <div>
    <h5 class="mb-3">Mis Pedidos</h5>
    <div v-if="orders.length === 0" class="text-muted">
      No tienes pedidos aún. <nuxt-link to="/shop">Ir a la tienda</nuxt-link>
    </div>
    <div v-for="order in orders" :key="order.id" class="order-card mb-3 p-3 border rounded">
      <div class="d-flex justify-content-between">
        <div>
          <code>{{ order.reference }}</code>
          <div class="text-muted small">{{ order.created_at }}</div>
        </div>
        <span :class="`badge bg-${statusColor(order.status)}`">
          {{ statusLabel(order.status) }}
        </span>
      </div>
      <div class="mt-2">
        <div v-for="item in order.items" :key="item.id" class="text-sm">
          {{ item.quantity }}x {{ item.product_title }}
          <span v-if="item.size" class="text-muted">({{ item.size }})</span>
        </div>
      </div>
      <div class="mt-2 fw-bold">Total: {{ order.total_formatted }}</div>
    </div>
  </div>
</template>
```

- [ ] **Step 3: Actualizar profile-password.vue**

```vue
<script setup lang="ts">
import { toast } from 'vue3-toastify'

const { apiFetch } = useApi()
const loading = ref(false)
const error = ref('')

const form = ref({
  current_password: '',
  password: '',
  password_confirmation: '',
})

const handleUpdate = async () => {
  if (form.value.password !== form.value.password_confirmation) {
    error.value = 'Las contraseñas no coinciden.'
    return
  }

  loading.value = true
  error.value = ''

  try {
    const res = await apiFetch<{ message: string }>('/profile/password', {
      method: 'PUT',
      body: form.value,
    })
    toast.success(res.message)
    form.value = { current_password: '', password: '', password_confirmation: '' }
  } catch (err: any) {
    const errors = err?.data?.errors
    error.value = errors
      ? Object.values(errors).flat().join('. ')
      : (err?.data?.message ?? 'Error al cambiar contraseña.')
    toast.error(error.value)
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <form @submit.prevent="handleUpdate">
    <div class="row g-3">
      <div class="col-12">
        <label class="form-label">Contraseña actual</label>
        <input v-model="form.current_password" type="password" class="form-control" required />
      </div>
      <div class="col-md-6">
        <label class="form-label">Nueva contraseña</label>
        <input v-model="form.password" type="password" class="form-control" required minlength="8" />
      </div>
      <div class="col-md-6">
        <label class="form-label">Confirmar contraseña</label>
        <input v-model="form.password_confirmation" type="password" class="form-control" required />
      </div>
      <div class="col-12">
        <p v-if="error" class="text-danger">{{ error }}</p>
        <button type="submit" :disabled="loading" class="tp-btn">
          {{ loading ? 'Actualizando...' : 'Cambiar contraseña' }}
        </button>
      </div>
    </div>
  </form>
</template>
```

- [ ] **Step 4: Agregar función saveToStorage en authStore**

En `app/pinia/useAuthStore.ts`, hacer público `saveToStorage` (ya está, solo verificar que es retornado):
```typescript
return {
  token,
  user,
  isAuthenticated,
  isAdmin,
  loadFromStorage,
  saveToStorage,  // <- asegurarse que está en el return
  login,
  register,
  logout,
  fetchMe,
}
```

---

### Task 4: Verificación final completa del sistema

- [ ] **Step 1: Test de registro y perfil**

1. Registrar usuario nuevo en `http://localhost:3000/register`
2. Ir a `/profile` → debe mostrar datos del usuario
3. Editar nombre y teléfono → guardar → verificar que persiste al recargar
4. Cambiar contraseña → hacer logout → login con nueva contraseña

- [ ] **Step 2: Test de pedido completo**

1. Login como `cliente@test.com`
2. Agregar Nike Tee al carrito en tamaño M
3. Ir a `/checkout` → llenar dirección → pagar con Wompi sandbox
4. Verificar confirmación en `/checkout/confirmacion`
5. Ir a `/profile` → tab Pedidos → ver el pedido con estado "pagado"
6. En Filament: marcar pedido como "enviado" → verificar que cambia en perfil Nuxt

- [ ] **Step 3: Test de encargo completo**

1. Ir al home → sección encargo → solicitar Nike Air Force 1 talla 42
2. En Filament: cotizar a $350.000 → guardar → wa_link generado
3. En perfil Nuxt: ver encargo con botón WhatsApp
4. En Filament: avanzar a en_camino → entregado
5. Verificar tracking en perfil del cliente

- [ ] **Step 4: Commit final**

```bash
# Laravel
cd C:/Users/GODSF/Herd/hauled-api
git add .
git commit -m "feat: perfil completo - datos, pedidos, contraseña + UserResource Filament"

# Nuxt
cd C:/Users/GODSF/Herd/Hauled
git add .
git commit -m "feat: perfil Nuxt completamente funcional - info, pedidos, encargos, password"
```

---

### Verificación Final del Módulo 10 (y del sistema completo)

- [ ] `PUT /api/v1/profile` → actualiza datos del usuario
- [ ] `PUT /api/v1/profile/password` → cambia contraseña con validación de actual
- [ ] `GET /api/v1/profile/orders` → historial de pedidos del usuario
- [ ] `GET /api/v1/profile/encargos` → historial de encargos con estados
- [ ] `/profile` en Nuxt tiene 4 tabs funcionales: Info, Pedidos, Encargos, Seguridad
- [ ] Filament `/admin/users` → lista usuarios con conteo de pedidos/encargos
- [ ] Flujo completo de inicio a fin sin errores

---

## Sistema HAULED — Completado ✅

| Módulo | Status |
|--------|--------|
| 1 — Setup Laravel | ✅ |
| 2 — Migraciones + Modelos | ✅ |
| 3 — Auth API | ✅ |
| 4 — Productos API | ✅ |
| 5 — Filament + Productos | ✅ |
| 6 — Categorías + Marcas | ✅ |
| 7 — Auth Nuxt | ✅ |
| 8 — Pedidos + Wompi | ✅ |
| 9 — Encargos | ✅ |
| 10 — Perfil | ✅ |
